import React from "react";
import {Text, StyleSheet, View, Image} from 'react-native';

function App() {
    reutnr (
    <View style={styles.container}>
      <Text> This is search Bar, Hey you are not displaying on my screen </Text>
    </View>
    )
};

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor:  '#fff',
        alignItems: 'center',
        justifyContent: 'center',
    }
});

export default App;